<Window x:Class="StudentInfoSystem.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentInfoSystem"
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Name: </Label>
        <TextBox x:Name="NameTxt" Width="200" Grid.Row="0" Grid.Column="1"/>

        <Label Grid.Row="1" Grid.Column="0">Password:</Label>
        <PasswordBox Width="200" x:Name="PassTxt" Grid.Row="1" Grid.Column="1"/>

        <TextBlock x:Name="ErrorTxt" Visibility="Hidden" Grid.Row="2" Grid.Column="1"/>

        <Button Grid.Row="3" Grid.Column="1" Click="Button_Click">Login</Button>
    </Grid>
</Window>
